import React from 'react';
import Cards from '../cards/cards';

const Deck = ({ deck }) => {

 /*
  -- divide the deck in half
  -- place each half in it's own cards array
   -- eg. computerCards, userCards
  -- send the cards array to Cards

 */


 return (
  <div>Deck</div>
 );
};

export default Deck;