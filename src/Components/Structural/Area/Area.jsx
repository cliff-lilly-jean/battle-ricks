import React from 'react';
import Tile from '';

const Area = ({ width, height }) => {
    // The area takes a width and height; converts them into an array of Tile

    return (
        <div>Area</div>
    );
};

export default Area;