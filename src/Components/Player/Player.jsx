import React from 'react';
import Deck from '../Deck/Deck';

const Player = () => {

 // The Player component has a Deck.

 // * Attributes
 // The Player can draw from the Deck
 // The Player can return to the Deck
 // The Player can place from the Deck to the Board
 // The Player has a health point state
 // The health point state can be subtracted from
 // The Player has a boolean isUser state


 return (
  <Deck />
 );
};

export default Player;